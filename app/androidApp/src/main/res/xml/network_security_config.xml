<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 生产只信任系统 CA。局域网分享、MCP、WebDAV/S3 仍可能走 HTTP，因此保留明文。 -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- 仅 debuggable 构建信任用户 CA，便于调试抓包；正式包不可被用户 CA 中间人。 -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
