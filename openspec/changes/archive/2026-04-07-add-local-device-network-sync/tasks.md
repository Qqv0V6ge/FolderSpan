## 1. Specification
- [ ] 1.1 新增 `manage-sync-tasks` 能力规范，定义同步入口、页面与任务模型。
- [x] 1.2 明确 `Local/Device/Network` 三端互相同步能力与 6 个跨类型方向，并补充 `Network -> Network`。
- [ ] 1.3 明确“单任务单向同步”语义、冲突策略与错误处理规则。

## 2. Data & Domain
- [ ] 2.1 新增同步任务配置模型（来源、目标、策略、调度、开关）。
- [ ] 2.2 新增同步运行记录模型（运行状态、统计、失败明细）。
- [ ] 2.3 复用并引用现有 Device/Network 连接与凭据，不重复存储敏感信息。

## 3. UI
- [ ] 3.1 实现“新增同步”页面（创建/编辑复用）。
- [x] 3.2 实现“同步管理”页面（列表、筛选、批量与行内操作）。
- [ ] 3.3 实现运行详情面板（进度、速度、结果、失败项重试）。

## 4. Execution
- [ ] 4.1 实现手动触发与定时触发（固定间隔/cron）。
- [ ] 4.2 实现增量复制执行器（目录递归、文件级结果）。
- [ ] 4.3 实现冲突策略（覆盖/跳过/重命名）与“应用到全部”。
- [ ] 4.4 实现取消任务、失败重试、单任务运行锁。
- [ ] 4.5 实现 `Device <-> Network` 本机中转策略。
- [x] 4.6 实现 `Network -> Network` 本机中转策略，并阻止同端点目录嵌套复制。

## 5. Validation
- [ ] 5.1 为现有 6 个跨类型方向及 `Network -> Network` 补充最小验收用例（成功、冲突、断连重试）。
- [x] 5.2 运行并通过相关模块测试（至少 `:shared:jvmTest`）。
- [x] 5.3 运行 `openspec validate add-local-device-network-sync --strict`。
