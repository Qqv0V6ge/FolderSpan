## 1. 可信设备模型与身份边界

- [x] 1.1 将 WebSocket presence/revision 模型替换为会话级账号设备可信快照，包含会话代次、获取时间、过期时间和最小可验证身份字段
- [x] 1.2 实现可信快照的新鲜度、账号切换、排除本机、设备移除和 fail-closed 纯决策，并补充聚焦单元测试
- [x] 1.3 增加应用内部的设备身份签名与验证边界，确保私钥自动管理且不进入设置同步、持久化快照或日志
- [x] 1.4 定义版本化 `lan-discovery`/`lan-connect` 证明、规范化签名载荷和显式授权模式，不保留开发阶段旧协议互操作
- [x] 1.5 实现有界 nonce 防重放缓存、用途隔离、有效时间窗和脱敏错误分类，并覆盖过期、重放、错目标和错请求体测试

## 2. 账号设备列表刷新

- [x] 2.1 扩展前端账号设备 DTO/mapper 以消费最终后端契约提供的身份验证材料，缺少必要字段的设备不得进入自动信任集合
- [x] 2.2 为账号设备列表增加绕过个人资料 30 天缓存的授权专用刷新路径，并保持个人资料页面原缓存行为不变
- [x] 2.3 实现 5 分钟可信有效期、near-expiry 判断和 single-flight 刷新，覆盖设置开启、登录成功、会话替换、前台恢复和扫描触发点
- [x] 2.4 使用会话代次丢弃旧账号迟到响应，并在退出登录、关闭设置及 401/403 时清空可信快照
- [x] 2.5 通过依赖注入把 `proMain` 的脱敏可信快照发布给 `core`，保持 `core` 不反向依赖 `proMain`
- [x] 2.6 为刷新成功、刷新失败仍在有效期、过期 fail-closed、并发合并和账号切换编写 repository/协调器测试

## 3. 局域网发现与自动连接

- [x] 3.1 扩展 LAN ping 请求以携带随机 challenge，并由响应设备生成绑定双方设备、端点/TLS 身份和响应摘要的发现证明
- [x] 3.2 在发现结果进入账号自动连接决策前验证成员资格、快照新鲜度和发现证明，禁止仅凭 `deviceKey` 自动连接
- [x] 3.3 将验证通过的候选接入现有 `DeviceState.connect(SocketDevice)`，保留本机排除、永久拒绝、证书检查和非账号发现行为
- [x] 3.4 增加按设备 in-flight 去重、失败退避和连接建立后的冗余任务取消，且不得写入永久 `AUTO_CONNECT` 策略
- [x] 3.5 覆盖可信发现、发现响应无证明、伪造标识、错误签名、重复扫描、永久拒绝和已连接设备的单元/集成测试

## 4. 入站自动同意与撤销

- [x] 4.1 扩展直接设备连接请求，由发起方生成绑定目标、路径、时间戳、nonce 和请求体摘要的 `lan-connect` 证明
- [x] 4.2 在 HTTP 入站授权中实现“永久拒绝 > 身份冲突/重放 > 同账号有效证明自动同意 > 现有流程”的固定优先级
- [x] 4.3 自动同意时优先复用有效的已有设备角色，否则使用内置最小权限角色，不增加用户角色配置项
- [x] 4.4 标记仅依赖临时账号信任建立的连接，并在关闭设置、会话结束或刷新发现设备被移除时取消待处理工作并关闭该类连接
- [x] 4.5 移除 WebRTC 授权对账号 WebSocket presence 的依赖，使其回到原有非账号授权流程，并验证现有 WebRTC 行为无回归
- [x] 4.6 覆盖有效证明自动同意、账号模式缺证明拒绝、标准模式人工授权、身份冲突拒绝、nonce 重放、永久拒绝、最小权限和撤销清理测试

## 5. 删除客户端账号设备 WebSocket 方案

- [x] 5.1 删除账号设备 WebSocket 票据 API 方法/DTO、WebSocket 协议模型、传输客户端和重连协调器
- [x] 5.2 删除 presence revision 注册表、连接状态 store、旧自动候选逻辑以及只服务于 WebSocket 在线快照的测试
- [x] 5.3 从各平台应用根生命周期和依赖注入中移除账号 WebSocket 启停绑定，改为启动/释放可信快照刷新协调器
- [x] 5.4 清理不再使用的账号 WebSocket 文案、错误分类和测试夹具，同时保留 WebRTC 等其他功能需要的通用 WebSocket 依赖
- [x] 5.5 增加网络层回归测试，断言启用功能后只刷新账号设备列表且不会调用票据接口或打开账号设备 WebSocket

## 6. 设置与前端文档

- [x] 6.1 复用现有本机布尔设置键并保持默认关闭、不参与 Pro 配置同步，将用户可见名称调整为“自动连接我的设备”
- [x] 6.2 将设置页收敛为一个开关和普通语言说明，删除票据、连接中、在线、退避、撤销和协议不兼容等持久通道状态展示
- [x] 6.3 确保未登录和刷新失败提示不暴露 token、接口、设备 ID、身份密钥、签名或协议术语，且不新增访问密钥/角色配置入口
- [x] 6.4 删除或改写账号设备 WebSocket 前端文档，更新设置同步说明，并按仓库规则同步 `md_descriptions_paths.md`
- [x] 6.5 更新设置状态、文案和同步白名单测试，覆盖默认值、持久化、登录前开启、即时关闭和 Pro 同步排除

## 7. 验证与交付

- [x] 7.1 运行 `:core:jvmTest`、`:proMain:jvmTest` 和 `:app:shared:jvmTest`，修复本变更导致的失败
- [x] 7.2 运行受影响的 Android、Desktop、JS 和 Wasm 编译任务，确认当前证明协议与身份边界跨平台可编译
- [x] 7.3 检查客户端源码和文档不再引用 `/devices/ws-tickets`、账号 `/devices/ws` 或 `folderspan.account-devices.v1`，同时确认后端仓库没有由本变更产生的工作区修改（保留既有修改）
- [x] 7.4 更新代码库知识图谱并复核变更差异，确保只包含客户端实现、测试和前端文档

## 8. 开发阶段协议收敛

- [x] 8.1 为直接连接请求和响应增加显式授权模式、拒绝 `UNSPECIFIED`，并禁止账号授权失败后降级到普通流程
- [x] 8.2 将账号设备身份 mapper 收敛为单一 snake_case 契约，删除旧字段形态和旧响应缺字段兼容测试
- [x] 8.3 同步规范与前端文档，并重新运行受影响测试和跨平台编译验证
