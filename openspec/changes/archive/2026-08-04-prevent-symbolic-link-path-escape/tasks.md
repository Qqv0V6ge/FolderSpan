## 1. 符号链接元数据与路径原语

- [x] 1.1 为 `FileSimpleInfo` 增加兼容的链接值和可信状态字段，并让 `withCopy`、本地文件映射及 HTTP/WebRTC 协议映射完整保留字段
- [x] 1.2 在各平台 `PathUtils` 实现符号链接探测和不经过链接的根边界校验，支持校验尚不存在的上传叶子
- [x] 1.3 增加旧 ProtoBuf 负载兼容、链接元数据映射、相似路径名和根外链接拒绝测试

## 2. 安全递归与删除

- [x] 2.1 让公共并行目录收集器把链接作为叶子，并提供不保留全部条目的流式统计模式
- [x] 2.2 将属性统计、复制、移动、删除清单及遗留 `PathUtils.traverse`、`Network.traverse` 接入统一不跟随策略
- [x] 2.3 将 JVM、Android、iOS 及内存后端的递归删除改为只删除链接本身，移除会跟随链接的便利 API
- [x] 2.4 增加外部目录链接、祖先循环、低内存统计和链接删除目标保持不变的回归测试

## 3. Android 递归入口与特权后端

- [x] 3.1 让普通、Root、Shizuku 列举返回可信链接元数据，并使特权遍历与删除采用不跟随语义
- [x] 3.2 修复 Android 文件拖拽目录收集，避免跟随链接或长期保留无界中间对象
- [x] 3.3 为 DocumentProvider 树遍历增加已访问目录标识和循环终止保护
- [x] 3.4 增加普通与特权后端行为契约测试并通过 Android 编译

## 4. 链接分享边界

- [x] 4.1 集中实现共享根到请求路径的安全解析，拒绝路径组件逃逸和共享根下的中间链接
- [x] 4.2 在浏览、单文件下载、批量下载和上传的每个实际 IO 入口复用安全解析结果
- [x] 4.3 增加浏览、下载、批量归档和上传经链接逃出共享根的服务端测试

## 5. 设备路径角色权限

- [x] 5.1 将角色授权路径的字符串前缀判断改为路径组件边界判断，并暴露请求操作可用的授权根
- [x] 5.2 在 `DevicePathService` 的列举、读取、创建、写入、删除、移动和重命名入口校验解析后的源/目标路径
- [x] 5.3 验证 HTTP 与 WebRTC 继续复用同一设备服务策略，并增加根内链接指向根外的权限回归测试

## 6. 验证与收尾

- [x] 6.1 运行 core/shared JVM 测试以及 Desktop、Android、JS、Wasm 编译验证，记录受宿主限制无法执行的目标
- [x] 6.2 搜索所有生产递归和路径授权入口，确认没有仍会跟随本地符号链接或只做字符串前缀判断的旁路
- [x] 6.3 严格校验 OpenSpec 变更并同步所有任务完成状态

验证记录：Linux 宿主已通过 core/shared JVM 测试、Desktop/Android/JS/Wasm 编译及新增 Android 特权删除定向测试；iOS 因 libcurl cinterop 仅支持在 macOS 工具链构建，当前宿主无法执行。完整 Android Host 测试受既有 Android API mock 环境影响有 79 个无关失败，新增定向测试单独通过。
