## 1. 公共模型与现状保护

- [x] 1.1 为现有“从剪贴板打开”文本/路径解析和各平台拖拽目标行为补充特征测试，确保后续重构不改变既有语义。
- [x] 1.2 在 `core/commonMain` 定义外部文件批次、跳过原因、目标快照、导入结果和资源租约标识等公共模型，并为去重与结果汇总添加单元测试。
- [x] 1.3 实现当前目录目标快照及粘贴权限预检，覆盖导航后目标不漂移、目标失效和不可粘贴目录测试。

## 2. 外部资源租约

- [x] 2.1 实现按批次和任务键引用计数的外部资源租约注册表，支持无任务立即释放及多任务最后引用释放。
- [x] 2.2 为文件系统暂存批次持久化最小租约元数据，并实现启动时保留可恢复任务来源、清理孤儿暂存目录的协调逻辑。
- [x] 2.3 将租约释放接入任务成功删除、取消、用户删除及不可重试终态，同时让可继续/可重试失败任务保留来源。
- [x] 2.4 添加资源生命周期测试，覆盖冲突对话框等待、部分任务完成、失败重试、取消、进程恢复协调和幂等清理。

## 3. 公共外部文件导入

- [x] 3.1 扩展复制粘贴执行器，使外部文件复制能够复用现有冲突规划并返回已创建任务键，且不改变内部复制/移动调用方行为。
- [x] 3.2 实现公共外部文件导入协调器，将准备后的顶层源、捕获的目标和资源租约原子地绑定到标准复制任务。
- [x] 3.3 处理空批次、部分准备失败、冲突全部跳过、目标失效和任务登记失败，并确保未被任务引用的资源及时释放。
- [x] 3.4 添加公共协调器测试，验证多文件、目录与空目录结构、覆盖/跳过/保留两者、目标快照、部分失败和标准任务控制语义。

## 4. 提取并复用拖拽文件准备逻辑

- [x] 4.1 从 Desktop 拖拽处理器提取 AWT 文件列表解析及本地 `FileSimpleInfo` 准备逻辑，保持拖拽到本地磁盘时原有的打开路径行为。
- [x] 4.2 从 Android `ShareHandler` 提取 URI 枚举、目录遍历、Share 注册和必要的流式暂存逻辑，使其返回可导入批次而不是强制切换磁盘。
- [x] 4.3 将 iOS `NSItemProvider` 加载与暂存流程整理为 UIDrop 和剪贴板均可调用的入口，并保持安全作用域与暂存清理配对。
- [x] 4.4 将 Web JS/Wasm 的浏览器文件解析、相对路径规范化和隐藏内存批次暂存整理为拖拽与粘贴共用逻辑。
- [x] 4.5 为四个平台补充拖拽回归测试或可自动化的适配层测试，验证重构后分享列表投递、目录结构和原有目标行为不变。

## 5. 平台剪贴板文件采集

- [x] 5.1 在 `app/shared` 增加独立于现有 `ClipboardContent` 文本模型的剪贴板文件准备契约，并保证文本路径和 URL 不进入复制源。
- [x] 5.2 实现 Desktop/JVM 剪贴板文件列表读取、真实文件过滤、稳定去重和公共批次转换。
- [x] 5.3 实现 Android `ClipData.Item.uri` 读取、内容类型检查、可读性诊断以及需要时的有界流式暂存，避免把 `content://` URI 降级为文本。
- [x] 5.4 实现 iOS `UIPasteboard.itemProviders` 文件准备并复用 iOS 外部项目暂存入口，覆盖多项目、失败项目和目录能力检测。
- [x] 5.5 在 Web JS 与 Wasm 安装可信 `paste` 事件读取，及时提取 `clipboardData.items/files` 并复用浏览器文件批次准备逻辑。
- [x] 5.6 为各平台采集器添加真实文件、多表示去重、仅文本、不可读条目、部分失败及目录不受支持的测试。
- [x] 5.7 增加剪贴板图片内容的公共命名、MIME 扩展名和表示优先级策略，并实现 Desktop AWT 图片与 Android 图片内容 URI 的暂存转换。
- [x] 5.8 实现 iOS `UTType.image`/`UIPasteboard.image` 与 Web JS/Wasm `image/* Blob` 的图片文件转换，并复用现有资源租约。
- [x] 5.9 添加图片剪贴板测试，覆盖生成文件名、真实文件优先、图片 Blob/位图、重复表示、无效图片和暂存清理。

## 6. 交互入口与反馈

- [x] 6.1 实现共享剪贴板文件粘贴控制器，负责目标预检/快照、忙碌与重复事件抑制、平台准备调用、公共导入和结构化结果反馈。
- [x] 6.2 在 AppDrawer 更多操作中增加与“从剪贴板打开”并列的“粘贴剪贴板文件”入口，并补充所有受支持语言资源和 UI 测试。
- [x] 6.3 为 Desktop 和 Web 增加文件浏览上下文的 `Ctrl/Cmd+V` 路由及可测试的焦点策略，确保搜索、重命名、对话框文本框和文件编辑器保留默认粘贴。
- [x] 6.4 将无文件、目标不可写、部分跳过、权限失败和全部准备失败映射到现有用户反馈组件，并确保日志不暴露敏感 URI 或文件内容。

## 7. 验证与交付

- [x] 7.1 运行 `:core:jvmTest` 与 `:app:shared:jvmTest`，修复公共协调、任务、资源租约和 Compose UI 回归。
- [x] 7.2 编译并测试 Android Debug，手动验证来自系统文件管理器的单文件、多文件、目录、权限失败及复制任务控制。
- [x] 7.3 编译并测试 Web JS/Wasm，验证文件粘贴、文本输入焦点保护、多文件、部分失败和内存批次清理。
- [x] 7.4 编译 iOS framework 并通过 Xcode 验证文件提供器多项目、暂存生命周期、失败反馈及目标切换竞态。
- [x] 7.5 在 Desktop 验证本地、设备和网络磁盘的快捷键/显式粘贴、冲突选择、暂停/取消/重试以及原有拖拽行为。
- [x] 7.6 运行严格 OpenSpec 校验并记录四个平台的测试结果与仍受平台限制的目录剪贴板能力。
